#include<stdio.h>
#include<stdlib.h>
struct node
{
    int info;
    struct node*prev*next;
}
struct node*temp;
if(struct==null)
{
    printf("\n list Is EMpty");
}
temp=start;
printf("\n The Doubly Linked List is:");
while(temp==null)
{
    printf("\n %d"temp->info);
    temp=temp->next;
}
void insert_at_front()
{

}
